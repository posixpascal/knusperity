import { ChatContext } from "./chat.machine";

export const checkout = (context: ChatContext) => {
  // Store order temporarily
  // Get open delivery times
  // Get confirmation from users
  // Prepare order, add items
  // if product missing, ask user for replacement or remove from order and revert back to previous state
  // confirm cart
  // list prices
  // get confirmation from users
  // dispatch order
  // use credit card
  // send chat confirmation
};
